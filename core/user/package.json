{
  "name": "@rideshare/user",
  "version": "0.0.0",
  "author": "gossi",
  "license": "MIT",
  "private": true,
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "start": "yarn build -w",
    "build": "npm-run-all build:*",
    "build:js": "swc ./* -d dist/",
    "build:types": "tsc -d --emitDeclarationOnly"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.55",
    "@swc/core": "^1.2.122",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.5.4"
  }
}
